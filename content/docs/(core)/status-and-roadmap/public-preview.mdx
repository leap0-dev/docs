---
title: Public Preview
description: What to expect while Leap0 is in public preview.
slug: public-preview
---

import { env } from '@/lib/content';

Leap0 is free to use during the public preview period.

During this time:

- Breaking changes may happen as we improve the platform.
- Short periods of downtime may occur.

## Report a bug

Email us at <a href={`mailto:${env.LEAP0_CONTACT_EMAIL}`}>{env.LEAP0_CONTACT_EMAIL}</a> with steps to reproduce and any relevant logs.
